<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="12dp">

    <!-- Timeline Track -->
    <View
        android:id="@+id/timelineTrack"
        android:layout_width="2dp"
        android:layout_height="0dp"
        android:background="@color/primaryBlue"
        android:alpha="0.2"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/guideline" />

    <!-- Stop Indicator -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/stopIndicator"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        app:shapeAppearanceOverlay="@style/Circle"
        app:srcCompat="@drawable/ic_circle"
        app:tint="@color/primaryBlue"
        app:layout_constraintStart_toStartOf="@id/guideline"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Stop Name -->
    <TextView
        android:id="@+id/tvStopName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        app:layout_constraintStart_toEndOf="@id/stopIndicator"
        app:layout_constraintTop_toTopOf="@id/stopIndicator"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/stopIndicator"
        android:contentDescription="@string/stop_item_description"/>

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_begin="62dp" />

</androidx.constraintlayout.widget.ConstraintLayout>